\vspace{5pt} \hrule
\begin{lstlisting}[caption={Алгоритм кодирования, близкого к оптимальному}, label=p_216, escapechar=\%]
%\noindent\textbf{Вход:} $double P[n]$ - массив вероятностей появления букв в сообщении, упорядоченный по невозрастанию; $1 \geq P[1] \geq \ldots P[i] > 0$, $P[1]+\ldots +P[n]=1$.\\%
%\textbf{Выход:} $bool C[n][L]$ - массив элементарных кодов.%
\end{lstlisting}
